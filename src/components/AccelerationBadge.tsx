import * as Tooltip from "@radix-ui/react-tooltip";import {Cpu,Gauge} from "lucide-react";import type {AccelerationRecord} from "@/lib/types";
export function AccelerationBadge({records}:{records:AccelerationRecord[]}){const active=records.some(r=>r.state==="active"),software=records.some(r=>r.state==="software");const label=active?"GPU active":software?"CPU encoding":"Acceleration unknown";return <Tooltip.Provider><Tooltip.Root><Tooltip.Trigger asChild><button className="inline-flex items-center gap-1.5 rounded-full border bg-card px-2.5 py-1 text-xs" aria-label={label}>{active?<Gauge size={14}/>:<Cpu size={14}/>} {label}</button></Tooltip.Trigger><Tooltip.Portal><Tooltip.Content sideOffset={6} className="z-50 max-w-xs rounded-md border bg-card p-3 text-xs shadow-xl">{records.map((r,i)=><div key={`${r.component}-${i}`}><b>{r.component.replace("_"," ")}:</b> {r.state}{r.implementation?` (${r.implementation})`:""}{r.reason?` — ${r.reason}`:""}</div>)}</Tooltip.Content></Tooltip.Portal></Tooltip.Root></Tooltip.Provider>}
