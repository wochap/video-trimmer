import {useCallback,useEffect,useRef,useState} from "react";import {open,save} from "@tauri-apps/plugin-dialog";import {getCurrentWindow} from "@tauri-apps/api/window";import {listen} from "@tauri-apps/api/event";import {FolderOpen,Pause,Play,Scissors,Upload} from "lucide-react";import {backend} from "@/lib/backend";import {clampRange,frameStepMicros,microsToSeconds,secondsToMicros} from "@/lib/time";import type {AccelerationRecord,ExportProgress,LaunchOptions,VideoMetadata} from "@/lib/types";import {Button} from "@/components/ui/button";import {Progress} from "@/components/ui/progress";import {ConfirmCancel} from "@/components/ui/alert-dialog";import {Timeline} from "@/components/Timeline";import {AccelerationBadge} from "@/components/AccelerationBadge";
type Phase="empty"|"loading"|"ready"|"exporting"|"error";const mp4=(p:string)=>p.toLowerCase().endsWith(".mp4");
export default function VideoTrimmer(){const [phase,setPhase]=useState<Phase>("empty"),[video,setVideo]=useState<VideoMetadata|null>(null),[error,setError]=useState(""),[drag,setDrag]=useState(false),[start,setStart]=useState(0),[end,setEnd]=useState(0),[playhead,setPlayhead]=useState(0),[playing,setPlaying]=useState(false),[previewOk,setPreviewOk]=useState(false),[progress,setProgress]=useState<ExportProgress|null>(null),[confirm,setConfirm]=useState(false),[launch,setLaunch]=useState<LaunchOptions>({input:null,output:null,force:false,verbose:false}),[acceleration,setAcceleration]=useState<AccelerationRecord[]>([]);const player=useRef<HTMLVideoElement>(null),cancelRequested=useRef(false);const step=frameStepMicros(video?.frameRate??30);
const load=useCallback(async(path:string)=>{if(phase==="exporting")return;if(!mp4(path)){setError("Choose exactly one local MP4 file.");setPhase(video?"ready":"error");return}setPhase("loading");setError("");try{const next=await backend.loadInput(path);setVideo(next);setStart(0);setEnd(next.durationMicros);setPlayhead(0);setPreviewOk(false);setAcceleration(next.playbackAcceleration);setPhase("ready")}catch(e){setError(String(e));setPhase(video?"ready":"error")}},[phase,video]);
const pick=useCallback(async()=>{const p=await open({multiple:false,directory:false,filters:[{name:"MP4 video",extensions:["mp4"]}]});if(typeof p==="string")await load(p)},[load]);
useEffect(()=>{backend.launchOptions().then(o=>{setLaunch(o);if(o.input)void load(o.input)}).catch(e=>{setError(String(e));setPhase("error")})},[]);
useEffect(()=>{let stop:undefined|(()=>void);getCurrentWindow().onDragDropEvent(e=>{if(phase==="exporting")return;if(e.payload.type==="over")setDrag(true);else if(e.payload.type==="leave")setDrag(false);else {setDrag(false);const paths=e.payload.paths;if(paths.length===1&&mp4(paths[0]))void load(paths[0]);else {setError("Drop exactly one MP4 file.");if(!video)setPhase("error")}}}).then(u=>stop=u);return()=>stop?.()},[load,phase,video]);
useEffect(()=>{const stops=[listen<ExportProgress>("export-progress",e=>setProgress(e.payload)),listen<AccelerationRecord[]>("acceleration-update",e=>setAcceleration(e.payload))];return()=>{for(const p of stops)void p.then(u=>u())}},[]);
useEffect(()=>{if(!previewOk)return;const refresh=()=>void backend.playbackAcceleration().then(records=>setAcceleration(previous=>[...records,...previous.filter(r=>r.component.startsWith("export_"))]));refresh();const id=window.setInterval(refresh,2000);return()=>window.clearInterval(id)},[previewOk]);
const seek=(v:number)=>{const n=Math.max(0,Math.min(v,video?.durationMicros??0));setPlayhead(n);if(player.current)player.current.currentTime=microsToSeconds(n)};const range=(s:number,e:number)=>{if(!video)return;const r=clampRange(s,e,video.durationMicros,step);setStart(r.start);setEnd(r.end);seek(Math.max(r.start,Math.min(playhead,r.end)))};const toggle=()=>{const el=player.current;if(!el)return;if(el.paused)void el.play();else el.pause()};
const trim=useCallback(async()=>{if(!video||!previewOk||phase==="exporting")return;let output=launch.output;if(!output)output=await save({defaultPath:`${video.path.replace(/\.mp4$/i,"")}-trimmed.mp4`,filters:[{name:"MP4 video",extensions:["mp4"]}]});if(!output)return;cancelRequested.current=false;setPhase("exporting");setProgress({fraction:0,outTimeMicros:0,attempt:"Preparing"});setError("");try{const result=await backend.exportVideo({input:video.path,output,startMicros:start,endMicros:end,force:launch.force});setAcceleration(result.acceleration)}catch(e){if(cancelRequested.current){await backend.exit(130);return}setError(String(e));setPhase("ready")}},[video,previewOk,phase,launch,start,end]);
const cancel=()=>phase==="exporting"?setConfirm(true):void backend.exit(130);useEffect(()=>{const key=(e:globalThis.KeyboardEvent)=>{const t=e.target;if(t instanceof HTMLElement&&(t.matches("input,textarea,select,[contenteditable=true]")||t.getAttribute("role")==="dialog"))return;if(e.ctrlKey&&e.key.toLowerCase()==="o"){e.preventDefault();void pick();return}if(phase!=="ready")return;let handled=true;if(e.key===" ")toggle();else if(e.key==="ArrowLeft")seek(playhead-(e.shiftKey?1_000_000:step));else if(e.key==="ArrowRight")seek(playhead+(e.shiftKey?1_000_000:step));else if(e.key.toLowerCase()==="i")range(playhead,end);else if(e.key.toLowerCase()==="o")range(start,playhead);else if(e.key==="Enter")void trim();else if(e.key==="Escape")cancel();else handled=false;if(handled)e.preventDefault()};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[phase,pick,playhead,step,start,end,trim]);
return <main className="relative flex h-full flex-col bg-background p-4 sm:p-6">{drag&&<div className="pointer-events-none absolute inset-3 z-30 grid place-items-center rounded-xl border-2 border-dashed border-primary bg-background/90 text-xl font-semibold">Drop MP4 to open</div>}<header className="mb-3 flex items-center justify-between"><div><h1 className="font-semibold">Video Trimmer</h1>{video&&<p className="max-w-[55vw] truncate text-xs text-muted-foreground">{video.path}</p>}</div><AccelerationBadge records={acceleration}/></header>{!video&&(phase==="empty"||phase==="error"||phase==="loading")?<section className="grid flex-1 place-items-center"><button onClick={()=>void pick()} disabled={phase==="loading"} className="flex min-h-64 w-full max-w-2xl flex-col items-center justify-center gap-4 rounded-xl border-2 border-dashed bg-card p-8 text-center hover:border-primary"><Upload size={42}/><span className="text-xl font-medium">{phase==="loading"?"Inspecting video…":"Open an MP4 video"}</span><span className="text-sm text-muted-foreground">Click, press Enter, use Ctrl+O, or drop one file</span></button></section>:video&&<section className="flex min-h-0 flex-1 flex-col gap-4"><div className="relative flex min-h-0 flex-1 items-center justify-center overflow-hidden rounded-lg bg-black"><video key={video.previewUrl} ref={player} src={video.previewUrl} controls className="max-h-full max-w-full" onClick={toggle} onLoadedMetadata={()=>setPreviewOk(true)} onError={()=>{setPreviewOk(false);setError("This MP4 was inspected successfully, but WebKit/GStreamer cannot preview it.")}} onTimeUpdate={e=>setPlayhead(secondsToMicros(e.currentTarget.currentTime))} onPlay={()=>setPlaying(true)} onPause={()=>setPlaying(false)}/></div><div className="rounded-lg border bg-card p-4"><div className="mb-3 flex items-center justify-between"><Button variant="ghost" size="icon" aria-label={playing?"Pause":"Play"} onClick={toggle}>{playing?<Pause/>:<Play/>}</Button><span className="text-xs text-muted-foreground">{video.width}×{video.height} · {video.codec} · {video.frameRate.toFixed(3)} fps{video.hasAudio?" · audio":" · silent"}</span><Button variant="ghost" size="sm" onClick={()=>void pick()} disabled={phase==="exporting"}><FolderOpen size={15}/> Replace</Button></div><Timeline duration={video.durationMicros} start={start} end={end} playhead={playhead} step={step} thumbnails={video.thumbnails} onSeek={seek} onRange={range}/>{video.thumbnailWarning&&<p className="mt-2 text-xs text-muted-foreground">{video.thumbnailWarning}</p>}{phase==="exporting"&&<div className="mt-4"><Progress value={(progress?.fraction??0)*100}/><p className="mt-1 text-xs text-muted-foreground">{progress?.attempt} · {Math.round((progress?.fraction??0)*100)}%</p></div>}</div></section>}<footer className="mt-4 flex items-center justify-between"><p role="status" aria-live="polite" className="text-sm text-destructive">{error}</p><div className="flex gap-2"><Button variant="secondary" onClick={cancel}>Cancel</Button><Button onClick={()=>void trim()} disabled={!video||!previewOk||phase==="exporting"}><Scissors size={16}/> Trim</Button></div></footer><ConfirmCancel open={confirm} onOpenChange={setConfirm} onConfirm={()=>{cancelRequested.current=true;void backend.cancelExport()}}/></main>}
